{
  "$comment": [
    "Operator-owned Casefile policy for scanning plugins/coeval. Pass it explicitly:",
    "  casefile scan plugins/coeval --config casefile.config.json --strict --fail-on warning",
    "Suppressed findings stay in the report as reviewed evidence; they only stop counting toward the gate."
  ],
  "ignore": [
    {
      "ruleId": "capability/network-call",
      "path": "skills/coeval-audit/scripts/",
      "reason": "coeval-submit.mjs is the audit skill's HTTP transport to the user's own Coeval instance; the URL comes from the connection the user created and credentials never enter chat or Git. Reviewed 2026-09-15."
    }
  ]
}
